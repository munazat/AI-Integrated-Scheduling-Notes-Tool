{
  "name": "ai-integrated-scheduling-notes-tool",
  "version": "0.1.0",
  "description": "AI-powered meeting notes and summarization tool with Google Calendar integration",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "start": "npm run start --workspaces",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "eslint": "^8.55.0"
  }
}
